/** Timings da intro (JS) — espelham site-intro.css na primeira migração */
export const SITE_INTRO = {
  playMs: 1400,
  exitMs: 650,
} as const;

/** Timings da recalibração do catálogo */
export const CATALOG_RECALIBRATE = {
  clearMs: 320,
  sweepMs: 680,
  revealMs: 880,
  resetAtSweepMs: 220,
} as const;
